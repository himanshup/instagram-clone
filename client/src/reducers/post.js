import {} from "../constants/action-types";

const initialState = {
  posts: {}
};
export default function(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
